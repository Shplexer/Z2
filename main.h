#pragma once

#include <vector>
#include <any>
#include "classes.h"
#include "functions.h"
#include "interface.h"
#include "fileFunc.h"

enum class fileMenuChoice{ test = 1, file, empty, exit };
enum class editMenuChoice{ add = 1, remove, save, goBack, exit};
